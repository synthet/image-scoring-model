# `.claude/` — generated agent mirror

**Generated** from [`.cursor/`](../.cursor/INDEX.md) by `scripts/sync_assistant_trees.py`.

| Path | Semantics |
|------|-----------|
| `commands/` | Copy of `.cursor/commands/` |
| `skills/` | Copy of `.cursor/skills/` |
| `rules/*.md` | Selected rules from `.cursor/rules/*.mdc` (see `MIRROR_RULES`) |

Never hand-edit this tree. Local-only files such as `settings.local.json` stay untracked.
