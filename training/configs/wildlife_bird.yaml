# Dataset root — override path when training
path: data/wildlife_bird

train: images/train
val: images/val

kpt_shape: [6, 3]
flip_idx: [0, 2, 1, 3, 5, 4]

names:
  0: bird

kpt_names:
  0:
    - beak
    - left_eye
    - right_eye
    - head_top
    - left_shoulder
    - right_shoulder
