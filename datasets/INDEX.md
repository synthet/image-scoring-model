# `datasets/` — local smoke fixtures

**Gitignored.** Tiny datasets for Ultralytics / pipeline smoke checks (e.g. `coco8/`).

Not the CUB training corpus — that lives in [`data/`](../data/INDEX.md).  
Do not commit these fixtures; SAFETY forbids shipping datasets in git.
